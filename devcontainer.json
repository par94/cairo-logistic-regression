// 🇨🇭 swiss knife of cairo tools
// - scarb
// - katana
// - 


{
    "image": "mcr.microsoft.com/devcontainers/universal:2",
    "hostRequirements": {
      "cpus": 4
    },
    "waitFor": "onCreateCommand",
    "updateContentCommand": "python3 -m pip install -r notebooks/requirements.txt",


    
    // install scarb
    // curl --proto '=https' --tlsv1.2 -sSf https://docs.swmansion.com/scarb/install.sh | sh 
    
    // install katana
    // mkdir ~/.dojo && cd ~/.dojo & git clone https://github.com/dojoengine/dojo & cd dojo
    // install rust
    // curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
    // source ~/.bashrc 
    // cargo install --path ./crates/katana --locked --force

    // install starkli
    // curl https://get.starkli.sh | sh
    // source ~/.bashrc 
    // starkliup
    // 

    "postCreateCommand": "",
    "customizations": {
      "codespaces": {
        "openFiles": []
      },
      "vscode": {
        "extensions": [
          "ms-toolsai.jupyter",
          "ms-python.python",
          ""
        ]
      }
    }
  }